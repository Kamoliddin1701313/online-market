/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["olx.digitallaboratory.uz"],
  },
  // images: {
  //   remotePatterns: [{ hostname: "olx.digitallaboratory.uz" }],
  // },
};

export default nextConfig;
